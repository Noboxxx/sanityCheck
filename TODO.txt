TODO:
    - buttons
    - info dialog (detailed information on the check: time, ...)
    - progress bar (check all)
    - mixin
    - dpi
    - cancel build ?!
    - standalone tests
    - enable button if works on selection
    - group checks in interface ?!